name: Main Branch CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.x  # Adjust to your project's .NET version

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build src/AnotherJsonLib.sln --configuration Release --no-restore  # No need to specify the solution file path here

      - name: Test
        run: dotnet test --configuration Release --no-build --verbosity normal src/AJL.Tests
